# 📅 9天详细开发计划（加强版）

**每个步骤都有具体操作指导，跟着做就行！**

---

# Day 1：熟悉 Roblox Studio

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 界面认识 | 30分钟 |
| Session 2 | 基础操作练习 | 30分钟 |
| Session 3 | 小作品挑战 | 20分钟 |

---

## Session 1：界面认识 (30分钟)

### 1.1 启动项目 (5分钟)
```
1. 打开 Roblox Studio
2. 点击 "New" → 选择 "Baseplate"
3. 等待加载完成
4. File → Save to Roblox As...
5. 名称填: Bus65
6. 点 Create
```

### 1.2 认识主界面 (10分钟)

**看屏幕，找到这些区域：**

```
┌────────────────────────────────────────────────────────────┐
│  菜单栏: File | Home | Model | Test | View | Plugins       │
├─────────┬──────────────────────────────────┬───────────────┤
│         │                                  │               │
│Explorer │         3D 视口                  │  Properties   │
│(层级树) │      （游戏世界）                │   (属性)      │
│         │                                  │               │
│         │     这里放你的游戏物体           │  选中物体后   │
│         │                                  │  在这里改属性 │
│         │                                  │               │
├─────────┴──────────────────────────────────┴───────────────┤
│  Output (输出)  |  Command Bar (命令行)                     │
└────────────────────────────────────────────────────────────┘
```

**任务：用鼠标点一下每个区域，确认你知道它在哪**
- [ ] 找到 Explorer（左边）
- [ ] 找到 Properties（右边）
- [ ] 找到 3D 视口（中间大区域）
- [ ] 找到 Output（底部，View → Output 打开）

### 1.3 认识 Explorer 结构 (10分钟)

**在 Explorer 里展开看看：**

```
Workspace          ← 游戏世界，所有看得见的东西
├── Camera         ← 摄像机
├── Terrain        ← 地形
└── SpawnLocation  ← 玩家出生点

Players            ← 玩家列表（测试时会有）
Lighting           ← 灯光设置
ReplicatedStorage  ← 共享数据存储
ServerScriptService← 服务器脚本放这里
StarterGui         ← 玩家UI模板
StarterPack        ← 玩家初始工具
StarterPlayer      ← 玩家设置
```

**任务：**
- [ ] 点击 Workspace 左边的箭头展开
- [ ] 找到 SpawnLocation（出生点）
- [ ] 点击它，看右边 Properties 显示它的属性

### 1.4 视角控制 (5分钟)

**练习这些操作直到熟练：**

| 操作 | 方法 | 练习次数 |
|------|------|----------|
| 旋转视角 | 按住右键 + 移动鼠标 | 5次 |
| 移动视角 | 按住右键 + WASD | 5次 |
| 缩放 | 滚轮上下滚 | 5次 |
| 平移 | 按住中键 + 移动鼠标 | 3次 |
| 聚焦物体 | 选中物体按 F | 3次 |

---

## Session 2：基础操作练习 (30分钟)

### 2.1 放置方块 (5分钟)

```
1. 点击顶部菜单 "Home"
2. 找到 "Part" 按钮（有个立方体图标）
3. 点击它
4. 一个灰色方块出现在世界里！
```

**任务：放置 3 个方块**
- [ ] 第一个方块
- [ ] 第二个方块  
- [ ] 第三个方块

### 2.2 选择工具 (3分钟)

**在 Home 菜单找到这些工具：**

```
[Select] [Move] [Scale] [Rotate]
   ↓       ↓       ↓       ↓
  选择   移动    缩放    旋转
```

快捷键：
- 选择: 无（默认）
- 移动: Ctrl+2
- 缩放: Ctrl+3  
- 旋转: Ctrl+4

### 2.3 移动物体 (5分钟)

```
1. 点击 Move 工具（或按 Ctrl+2）
2. 点击选中一个方块
3. 出现红绿蓝三个箭头
   - 红色 = X轴（左右）
   - 绿色 = Y轴（上下）
   - 蓝色 = Z轴（前后）
4. 拖动箭头移动方块
```

**任务：**
- [ ] 把一个方块往上移动
- [ ] 把一个方块往左移动
- [ ] 把一个方块往前移动

### 2.4 缩放物体 (5分钟)

```
1. 点击 Scale 工具（或按 Ctrl+3）
2. 选中方块
3. 出现彩色小球
4. 拖动小球改变大小
   - 角落的球 = 整体缩放
   - 面上的球 = 单方向缩放
```

**任务：**
- [ ] 把一个方块变成扁平的薄片
- [ ] 把一个方块变成长条
- [ ] 把一个方块变大

### 2.5 旋转物体 (5分钟)

```
1. 点击 Rotate 工具（或按 Ctrl+4）
2. 选中方块
3. 出现三个彩色圆环
4. 拖动圆环旋转
```

**任务：**
- [ ] 把一个方块转 45 度

### 2.6 修改属性 (7分钟)

**选中一个方块，在 Properties 面板找到并修改：**

| 属性 | 在哪 | 改成什么 |
|------|------|----------|
| Name | 最上面 | "MyFirstPart" |
| BrickColor | Appearance 里 | 选个喜欢的颜色 |
| Material | Appearance 里 | 选 "Wood" |
| Size | Size 属性 | 改成 10, 2, 10 |
| Transparency | Appearance 里 | 0.5（半透明）|

**任务：**
- [ ] 改名字
- [ ] 改颜色（选一个你喜欢的）
- [ ] 改材质（试试 Brick, Wood, Metal, Glass）
- [ ] 改大小
- [ ] 改透明度

---

## Session 3：小作品挑战 (20分钟)

### 🏠 挑战：建一个小房子！

**要求：**
1. 4 面墙
2. 1 个屋顶
3. 1 个门洞（把一面墙留个口）
4. 1 个窗户（用透明方块）
5. 地板

**提示：**
- 墙: 用砖材质 (Brick)
- 屋顶: 用木材质 (Wood)
- 窗户: 用玻璃材质 (Glass) + Transparency = 0.3
- 用复制 (Ctrl+D) 快速做多面墙

**步骤：**
```
1. 做地板：扁平方块，Size = 20, 1, 20
2. 做第一面墙：Size = 20, 10, 1，立在地板边缘
3. Ctrl+D 复制，移到对面
4. 再做两面侧墙
5. 做屋顶：可以斜的或平的
6. 加个门：留个口或用不同颜色标记
7. 加窗户：小的透明方块
```

### ✅ Day 1 完成检查

- [ ] 能熟练移动视角
- [ ] 能放置 Part
- [ ] 能移动、缩放、旋转
- [ ] 能修改颜色和材质
- [ ] 完成了小房子！
- [ ] 保存项目 (Ctrl+S)

### 📸 截图保存

```
1. 把房子调到好看的角度
2. View → Screenshot
3. 保存到 screenshots 文件夹
4. 命名: day1-house.png
```

---

# Day 2：获取并研究巴士模型

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 找巴士模型 | 30分钟 |
| Session 2 | 研究模型结构 | 30分钟 |
| Session 3 | 添加驾驶座 | 30分钟 |

---

## Session 1：找巴士模型 (30分钟)

### 1.1 打开 Toolbox (2分钟)

```
View → Toolbox
```

一个新面板出现，里面有很多现成的模型！

### 1.2 搜索巴士模型 (15分钟)

**依次搜索这些关键词：**

| 搜索词 | 找什么 |
|--------|--------|
| London bus | 伦敦红色双层巴士 |
| Double decker | 双层巴士 |
| UK bus | 英国巴士 |
| Routemaster | 经典伦敦巴士 |
| Bus chassis | 巴士底盘（可能自带驱动）|

**选择标准：**
- ✅ 外观好看
- ✅ 像伦敦巴士（红色双层）
- ✅ 不要太复杂（零件太多）
- ✅ 有评分/收藏多的优先

### 1.3 插入模型 (3分钟)

```
1. 找到喜欢的模型
2. 点击它
3. 模型出现在游戏里！
4. 可能很大或很小，没关系
```

### 1.4 试插几个比较 (10分钟)

**至少试 3 个不同的巴士模型：**

| # | 模型名称 | 优点 | 缺点 | 评分 |
|---|----------|------|------|------|
| 1 | | | | /5 |
| 2 | | | | /5 |
| 3 | | | | /5 |

**选择最喜欢的那个，删除其他的！**

删除方法：选中模型 → 按 Delete 键

---

## Session 2：研究模型结构 (30分钟)

### 2.1 在 Explorer 里展开模型 (10分钟)

```
1. 在 Explorer 里找到巴士模型
2. 点击左边的箭头展开
3. 看里面有什么
```

**常见结构：**
```
Bus (Model)
├── Body (车身 - Part 或 MeshPart)
├── Wheel1 (轮子)
├── Wheel2
├── Wheel3
├── Wheel4
├── Seat (座位，可能有)
├── Door (门，可能有)
└── ...其他零件
```

**任务：画出你的巴士模型结构**
```
我的巴士 (Model)
├── 
├── 
├── 
├── 
└── 
```

### 2.2 认识零件类型 (10分钟)

**点击不同零件，看 Properties 里的类型：**

| 类型 | 图标 | 说明 |
|------|------|------|
| Part | 方块 | 基础形状 |
| MeshPart | 网格 | 复杂3D模型 |
| Model | 文件夹 | 零件组合 |
| VehicleSeat | 座位 | 驾驶座 |
| Seat | 座位 | 普通座位 |

### 2.3 找到主车身 (5分钟)

**主车身特征：**
- 最大的那个零件
- 一般叫 Body、Chassis、Main 之类
- 其他零件都连着它

**记录：**
```
我的巴士主车身名字是: ____________
```

### 2.4 检查是否有驾驶座 (5分钟)

在 Explorer 里搜索或找：
- VehicleSeat
- DriveSeat
- DriverSeat

**有驾驶座？** □ 有 □ 没有

---

## Session 3：添加/设置驾驶座 (30分钟)

### 3.1 如果没有驾驶座 - 添加一个 (15分钟)

```
1. 在 Explorer 里选中巴士 Model
2. 右键 → Insert Object → VehicleSeat
3. 一个座位出现了！
4. 用 Move 工具把它移到司机位置
   - 在巴士前部
   - 左边（英国是左舵！等等，英国是右舵...司机在右边！）
   - 调整高度让它在车厢内
```

**英国巴士司机位置：**
```
        前进方向
           ↑
    ┌──────────────┐
    │ 乘客    [司机]│  ← 司机在右边！
    │              │
    │   乘客区     │
    │              │
    └──────────────┘
```

### 3.2 如果有驾驶座 - 检查位置 (5分钟)

```
1. 在 Explorer 里点击 VehicleSeat
2. 按 F 聚焦到它
3. 看看位置对不对
4. 不对就用 Move 工具调整
```

### 3.3 设置 PrimaryPart (10分钟)

**这一步很重要！告诉游戏哪个是巴士的"中心"**

```
1. 在 Explorer 里点击巴士 Model（最上层）
2. 在 Properties 里找到 PrimaryPart
3. 点击属性框
4. 在 Explorer 里点击主车身（最大的那个 Part）
5. PrimaryPart 应该显示主车身的名字了
```

### 3.4 测试！(5分钟)

```
1. 按 F5 进入游戏
2. 走到巴士旁边
3. 按 Y 或点击座位上车
4. 能坐上去吗？
5. 按 F8 退出测试
```

**测试结果：**
- [ ] 能走到巴士旁边
- [ ] 能坐上驾驶座
- [ ] 坐上去视角正常

### ✅ Day 2 完成检查

- [ ] 找到了喜欢的巴士模型
- [ ] 研究了模型结构
- [ ] 有驾驶座（VehicleSeat）
- [ ] 设置了 PrimaryPart
- [ ] 测试能坐上去
- [ ] 保存项目

### 📝 记录

**我选择的巴士模型：**
```
模型名字: 
来源（谁做的）:
好看程度: ⭐⭐⭐⭐⭐
```

---

# Day 3：让巴士动起来 ⭐关键日

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 第一个脚本 | 40分钟 |
| Session 2 | 驾驶脚本 | 50分钟 |
| Session 3 | 调试优化 | 30分钟 |

---

## Session 1：第一个脚本！(40分钟)

### 1.1 什么是脚本？(5分钟)

**脚本 = 给电脑的指令清单**

```
想象你在教一个机器人开巴士：
1. 如果玩家按 W，就往前走
2. 如果玩家按 S，就往后走
3. 如果玩家按 A，就往左转
...

这就是脚本做的事！
```

### 1.2 创建第一个脚本 (10分钟)

```
1. 在 Explorer 里找到 VehicleSeat
2. 右键 VehicleSeat
3. Insert Object → Script
4. 一个新的 Script 出现在 VehicleSeat 下面！
5. 双击 Script 打开编辑器
```

**你会看到：**
```lua
print("Hello world!")
```

### 1.3 理解 print (10分钟)

**print 是"打印"的意思，会在 Output 显示文字**

把代码改成：
```lua
print("Hello! 我是巴士脚本！")
print("今天是开发的第三天！")
print("马上巴士就能动了！")
```

**测试：**
```
1. 按 F5 运行游戏
2. 看 Output 面板（底部）
3. 应该看到三行文字！
4. 按 F8 退出
```

### 1.4 理解错误信息 (10分钟)

**故意写错，看看会怎样：**

```lua
print("Hello)  -- 少了一个引号
```

按 F5，会看到红色错误信息！

**常见错误：**
| 错误类型 | 示例 | 解决 |
|----------|------|------|
| 缺引号 | print("Hello) | 加上 " |
| 缺括号 | print("Hello" | 加上 ) |
| 拼写错误 | prnit("Hi") | 改成 print |

**记住：错误不可怕，看错误信息，慢慢改！**

### 1.5 变量初体验 (5分钟)

```lua
-- 这是注释，不会运行，用来写笔记

local myName = "Bus 65"  -- 创建变量
local mySpeed = 50       -- 数字不用引号

print(myName)           -- 显示: Bus 65
print("速度是: " .. mySpeed)  -- 显示: 速度是: 50
```

**理解：**
- `local` = 创建变量
- `=` = 赋值
- `..` = 连接文字
- `--` = 注释

---

## Session 2：驾驶脚本 (50分钟)

### 2.1 准备工作 (5分钟)

**确保巴士结构正确：**
```
Bus (Model)           ← 要有 PrimaryPart 设置好
├── Body (或其他主车身名字)
├── VehicleSeat       ← 脚本放这里
│   └── Script        ← 我们的脚本
└── 其他零件...
```

**所有零件的 Anchored 属性必须是 false：**
```
1. 选中巴士所有零件（点 Model 然后 Ctrl+A）
2. 在 Properties 找 Anchored
3. 确保是 未勾选（false）
```

### 2.2 复制驾驶脚本 (15分钟)

**双击 Script 打开，删除所有内容，粘贴以下代码：**

```lua
--[[
    巴士驾驶脚本
    Day 3 制作
    
    WASD 控制方向
]]

local seat = script.Parent

-- 找到车身
local bus = seat:FindFirstAncestorOfClass("Model")
local body = bus and (bus.PrimaryPart or bus:FindFirstChildWhichIsA("BasePart"))

if not body then
    warn("⚠️ 找不到车身！请设置 Model 的 PrimaryPart")
    return
end

print("✅ 巴士脚本加载成功！")

-- ======= 在这里调整参数 =======
local MAX_SPEED = 45      -- 最大速度（改大跑更快）
local TURN_SPEED = 2      -- 转向速度（改大转更快）
-- =============================

-- 创建移动力
local bodyVelocity = Instance.new("BodyVelocity")
bodyVelocity.MaxForce = Vector3.new(0, 0, 0)
bodyVelocity.Velocity = Vector3.new(0, 0, 0)
bodyVelocity.Parent = body

-- 创建转向力
local bodyGyro = Instance.new("BodyGyro")
bodyGyro.MaxTorque = Vector3.new(0, 0, 0)
bodyGyro.P = 5000
bodyGyro.D = 500
bodyGyro.Parent = body

-- 当前角度
local currentAngle = 0

-- 每帧更新
game:GetService("RunService").Heartbeat:Connect(function()
    if seat.Occupant then
        local throttle = seat.Throttle  -- W/S
        local steer = seat.Steer        -- A/D
        
        -- 前进/后退
        if throttle ~= 0 then
            bodyVelocity.MaxForce = Vector3.new(30000, 0, 30000)
            bodyVelocity.Velocity = body.CFrame.LookVector * throttle * MAX_SPEED
        else
            bodyVelocity.MaxForce = Vector3.new(30000, 0, 30000)
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        end
        
        -- 转向
        if math.abs(throttle) > 0.1 then
            currentAngle = currentAngle - steer * TURN_SPEED
            bodyGyro.MaxTorque = Vector3.new(0, 50000, 0)
            bodyGyro.CFrame = CFrame.Angles(0, math.rad(currentAngle), 0)
        end
    else
        bodyVelocity.MaxForce = Vector3.new(0, 0, 0)
    end
end)

print("🚌 用 WASD 控制方向！")
```

### 2.3 第一次测试 (10分钟)

```
1. 按 F5 进入游戏
2. 走到巴士旁边，按 Y 上车
3. 按 W 前进
4. 按 S 后退
5. 按 A/D 转向
6. 能动吗？！
```

**可能遇到的问题：**

| 问题 | 原因 | 解决 |
|------|------|------|
| 完全不动 | Anchored = true | 取消所有零件的 Anchored |
| Output 显示找不到车身 | PrimaryPart 没设置 | 设置 Model 的 PrimaryPart |
| 车轮飞走 | 零件没连接好 | 用 Weld 连接所有零件 |
| 翻车 | 重心不对 | 降低车身或加重底部 |

### 2.4 焊接零件 (如果需要) (20分钟)

**如果巴士零件散开了，需要焊接：**

```
方法 1：手动焊接
1. 选中要焊接的零件（比如轮子）
2. 右键 → Insert Object → WeldConstraint
3. 在 Properties 里设置:
   - Part0 = 这个零件
   - Part1 = 主车身
4. 对每个零件重复
```

```
方法 2：用脚本批量焊接
1. 在 Model 下创建新 Script
2. 粘贴以下代码
3. 运行一次游戏（F5 然后 F8）
4. 删除这个脚本

代码：
```

```lua
local model = script.Parent
local primary = model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart")

for _, part in model:GetDescendants() do
    if part:IsA("BasePart") and part ~= primary then
        local weld = Instance.new("WeldConstraint")
        weld.Part0 = primary
        weld.Part1 = part
        weld.Parent = part
        part.Anchored = false
    end
end

print("✅ 焊接完成！零件数量: " .. #model:GetDescendants())
script:Destroy()  -- 自动删除这个脚本
```

---

## Session 3：调试优化 (30分钟)

### 3.1 调整速度 (10分钟)

**在脚本里找到这两行：**
```lua
local MAX_SPEED = 45      -- 最大速度
local TURN_SPEED = 2      -- 转向速度
```

**尝试不同数值：**

| MAX_SPEED | 效果 |
|-----------|------|
| 30 | 慢悠悠，像真公交 |
| 45 | 正常速度 |
| 60 | 比较快 |
| 100 | 太快了！ |

| TURN_SPEED | 效果 |
|------------|------|
| 1 | 转向很慢 |
| 2 | 正常 |
| 3 | 转向灵敏 |
| 5 | 太灵敏！ |

**记录你选择的数值：**
```
我的设置：
MAX_SPEED = ____
TURN_SPEED = ____
```

### 3.2 防止翻车 (10分钟)

**如果巴士容易翻，试试这些：**

1. **降低重心**
   - 把主车身往下移一点
   
2. **加宽轴距**
   - 把轮子往外移

3. **加重底部**
   - 选中底盘零件
   - 增加 Density（密度）属性
   
4. **限制旋转**
   在脚本里加：
   ```lua
   bodyGyro.MaxTorque = Vector3.new(10000, 50000, 10000)
   ```

### 3.3 开车绕圈测试 (10分钟)

**最终测试：**
- [ ] 能平稳启动
- [ ] 能正常转弯
- [ ] 能停下来
- [ ] 不会乱翻
- [ ] 速度感觉合适

### ✅ Day 3 完成检查

- [ ] 写了第一行代码！
- [ ] 巴士能前进后退
- [ ] 巴士能转向
- [ ] 调整了合适的速度
- [ ] 巴士不会翻车
- [ ] 保存项目

### 🎉 里程碑！

**恭喜！你有一辆能开的伦敦巴士了！**

录一个小视频：
1. 打开手机录屏
2. F5 进入游戏
3. 开着巴士转一圈
4. 保存到 screenshots 文件夹

---

# Day 4：建造道路

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 主道路 | 40分钟 |
| Session 2 | 人行道和细节 | 40分钟 |
| Session 3 | 测试优化 | 20分钟 |

---

## Session 1：主道路 (40分钟)

### 1.1 道路设计参考 (5分钟)

**英国道路特点：**
- 靠左行驶！
- 公交车道有时在左侧
- 人行横道是黄黑条纹
- 双黄线 = 不能停车

**我们的道路规格：**
```
        14 studs 总宽
    ←──────────────→
    ┌──────┬───────┐
    │ 7宽  │ 7宽   │
    │ ←   │   →  │
    │ 车道 │ 车道  │
    └──────┴───────┘
    
    加上两边人行道各 4 studs
    总宽: 14 + 4 + 4 = 22 studs
```

### 1.2 创建第一段道路 (10分钟)

```
1. Home → Part
2. 选中新 Part
3. 在 Properties 里修改：
   Name: Road_01
   Size: 14, 0.3, 100
   Position: 0, 0.15, 0
   Material: Asphalt
   Color: RGB(50, 50, 50) 深灰色
   Anchored: ✓ 勾选（道路不动）
```

**道路放好后应该是一条黑色长条**

### 1.3 延长道路 (10分钟)

```
1. 选中 Road_01
2. 按住 Alt，往前拖动
3. 复制出 Road_02
4. 继续复制，总共 5 段
5. 连成一条长路
```

**检查：道路之间不能有缝隙！**

### 1.4 整理 (5分钟)

```
1. 在 Explorer 里右键 Workspace
2. Insert Object → Folder
3. 重命名为 "Roads"
4. 把所有 Road_XX 拖进去
```

### 1.5 加中线 (10分钟)

```
1. 创建新 Part
2. 设置：
   Name: CenterLine_01
   Size: 0.3, 0.01, 100
   Position: 0, 0.31, 0（比路面高一点）
   Material: SmoothPlastic
   Color: 白色 RGB(255, 255, 255)
   Anchored: ✓
3. 复制延长，和道路一样长
4. 放进 Roads 文件夹
```

---

## Session 2：人行道和细节 (40分钟)

### 2.1 左侧人行道 (10分钟)

```
1. 创建 Part
2. 设置：
   Name: Pavement_L_01
   Size: 4, 0.4, 100
   Position: -9, 0.2, 0（道路左边）
   Material: Concrete
   Color: RGB(180, 180, 180) 浅灰
   Anchored: ✓
3. 复制延长
```

### 2.2 右侧人行道 (10分钟)

```
同样操作，Position 改成 (9, 0.2, 0)
```

### 2.3 路缘石 (10分钟)

**人行道边缘的凸起：**

```
1. 创建 Part
2. 设置：
   Name: Curb_L_01
   Size: 0.3, 0.15, 100
   Position: -7.15, 0.35, 0
   Material: Concrete
   Color: RGB(160, 160, 160)
   Anchored: ✓
```

### 2.4 加弯道（可选）(10分钟)

**90度转弯的做法：**

```
1. 做一个正方形路面 Size: 14, 0.3, 14
2. 放在两条直路的交叉处
3. 用圆弧或多个小段做弯道边缘
```

简单做法：直接用正方形拼接

---

## Session 3：测试优化 (20分钟)

### 3.1 测试驾驶 (10分钟)

```
1. F5 进入游戏
2. 把巴士开到道路上
3. 沿着道路开一圈
4. 检查：
   - 道路够宽吗？
   - 有没有卡住的地方？
   - 颜色看起来舒服吗？
```

### 3.2 调整 (10分钟)

**根据测试调整：**
- 道路太窄 → 加宽 Size 的 X 值
- 道路太滑 → 不用管，脚本控制的
- 道路颜色不对 → 调整 Color

### ✅ Day 4 完成检查

- [ ] 有一条完整的道路
- [ ] 有人行道
- [ ] 有中线
- [ ] 巴士能在路上正常行驶
- [ ] 道路整理进 Folder
- [ ] 保存项目

---

# Day 5：建造 Richmond 车站 ⭐重点日

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 参考收集 + 规划 | 20分钟 |
| Session 2 | 车站建筑 | 50分钟 |
| Session 3 | 公交站 | 50分钟 |

---

## Session 1：参考收集 (20分钟)

### 1.1 Google 搜索参考图

打开浏览器，搜索：
- "Richmond Station London exterior"
- "Richmond bus stop"
- "London bus shelter"
- "TfL bus stop sign"

### 1.2 简化设计

**真实 Richmond Station 特点：**
- 红砖建筑
- 拱门入口
- 蓝色 National Rail 标识

**我们简化版：**
```
┌────────────────────────┐
│    RICHMOND           │  ← 蓝底白字招牌
├────────────────────────┤
│                        │
│   ╭────────────╮       │  ← 拱门入口
│   │            │       │
│   │   入口     │       │
│───┴────────────┴───────│
│                        │
│       红砖墙           │
│                        │
└────────────────────────┘
```

---

## Session 2：车站建筑 (50分钟)

### 2.1 创建车站 Model (5分钟)

```
1. 右键 Workspace → Insert Object → Model
2. 重命名: Richmond_Station
```

### 2.2 主建筑 (15分钟)

```
1. 在 Richmond_Station 下创建 Part
2. 设置：
   Name: MainBuilding
   Size: 50, 15, 25
   Position: 50, 7.5, 0
   Material: Brick
   Color: RGB(180, 100, 80) 红砖色
   Anchored: ✓
```

### 2.3 拱门入口 (15分钟)

**方法：用两个 Part 做门框，中间留空**

```
左门框：
Name: DoorFrame_L
Size: 5, 10, 2
Position: 42.5, 5, -12
Material: Brick
Color: 和主建筑一样

右门框：
Name: DoorFrame_R
Size: 5, 10, 2
Position: 57.5, 5, -12

门顶：
Name: DoorTop
Size: 20, 3, 2
Position: 50, 11.5, -12
```

### 2.4 站名牌 (15分钟)

```
1. 创建 Part
   Name: StationSign
   Size: 20, 3, 0.5
   Position: 50, 14, -12.5
   Color: RGB(0, 0, 130) 深蓝色

2. 右键 StationSign → Insert Object → SurfaceGui
3. 选中 SurfaceGui，设置 Face = Front
4. 在 SurfaceGui 下插入 TextLabel
5. TextLabel 设置：
   Size: {1, 0}, {1, 0}
   Text: RICHMOND
   TextColor3: 白色
   TextScaled: ✓
   BackgroundTransparency: 1
   Font: GothamBold
```

---

## Session 3：公交站 (50分钟)

### 3.1 创建公交站 Model (5分钟)

```
1. 在 Workspace 下创建 Model
2. 重命名: BusStop_Richmond
3. 设置 Attribute:
   - 点击 Model
   - Properties 最下面 Attributes
   - Add Attribute
   - Name: StopId, Type: String, Value: richmond
   - Add Attribute  
   - Name: StopName, Type: String, Value: Richmond
```

### 3.2 站台 (5分钟)

```
Part:
Name: Platform
Size: 4, 0.3, 15
Position: 15, 0.15, 0
Material: Concrete
Color: 浅灰
Anchored: ✓
放进 BusStop_Richmond
```

### 3.3 候车亭 (20分钟)

**亭顶：**
```
Part:
Name: ShelterRoof
Size: 4, 0.2, 12
Position: 17, 4, 0
Material: Glass
Transparency: 0.3
Color: 淡蓝色
Anchored: ✓
```

**柱子 x2：**
```
Part:
Name: ShelterPole_1
Size: 0.3, 4, 0.3
Position: 18.5, 2, -5
Material: Metal
Color: 深灰
Anchored: ✓

复制一个到 Position: 18.5, 2, 5
```

**后板（可选）：**
```
Part:
Name: ShelterBack
Size: 0.2, 3, 12
Position: 19, 2.5, 0
Material: Glass
Transparency: 0.5
Anchored: ✓
```

### 3.4 TfL 站牌 (15分钟)

**站牌杆：**
```
Part:
Name: SignPole
Size: 0.3, 4, 0.3
Position: 13, 2, -6
Material: Metal
Color: 深灰
Anchored: ✓
```

**红色圆盘：**
```
Part:
Name: SignHead
Shape: Cylinder
Size: 0.2, 1.8, 1.8
Position: 13, 4.3, -6
Orientation: 0, 0, 90
Color: RGB(255, 0, 0) TfL红
Anchored: ✓
```

**添加文字：**
```
1. 在 SignHead 上加 SurfaceGui
2. Face = Front
3. 加 TextLabel，写 "65"
4. 白字，大号
```

### 3.5 到站检测区 (5分钟)

```
Part:
Name: StopZone
Size: 15, 4, 20
Position: 5, 2, 0
Transparency: 1
CanCollide: false
Anchored: ✓
```

### ✅ Day 5 完成检查

- [ ] Richmond Station 建筑完成
- [ ] 有站名牌
- [ ] 公交站候车亭完成
- [ ] TfL 红色站牌完成
- [ ] StopZone 透明检测区完成
- [ ] 所有零件整理进 Model
- [ ] 保存项目

### 📸 截图

把 Richmond 站拍个好看的截图！

---

# Day 6：到站检测系统

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 检测脚本 | 40分钟 |
| Session 2 | UI 提示 | 40分钟 |
| Session 3 | 测试 | 20分钟 |

---

## Session 1：到站检测脚本 (40分钟)

### 1.1 创建脚本 (5分钟)

```
1. 在 BusStop_Richmond 下找到 StopZone
2. 右键 StopZone → Insert Object → Script
```

### 1.2 基础检测代码 (15分钟)

```lua
--[[
    到站检测脚本
    Day 6 制作
]]

local stopZone = script.Parent
local busStop = stopZone.Parent

local stopName = busStop:GetAttribute("StopName") or "Unknown"

print("✅ 站点检测器启动: " .. stopName)

-- 检测有东西进入
stopZone.Touched:Connect(function(hit)
    print("有东西碰到了检测区: " .. hit.Name)
end)
```

**测试：**
1. F5 运行
2. 走进检测区
3. 开车进检测区
4. 看 Output 有没有显示

### 1.3 只检测巴士 (20分钟)

**完整代码：**

```lua
local stopZone = script.Parent
local busStop = stopZone.Parent

local stopName = busStop:GetAttribute("StopName") or "Unknown"
local busInZone = false

print("✅ 站点检测器启动: " .. stopName)

stopZone.Touched:Connect(function(hit)
    -- 如果已经在站里，不重复触发
    if busInZone then return end
    
    -- 找到 Model（可能是巴士）
    local model = hit:FindFirstAncestorOfClass("Model")
    if not model then return end
    
    -- 检查是不是有 VehicleSeat（说明是车辆）
    local seat = model:FindFirstChildOfClass("VehicleSeat", true)
    if not seat then return end
    
    -- 是巴士！
    busInZone = true
    print("🚏 巴士到站: " .. stopName)
    
    -- 等一会才能再次触发
    wait(2)
    busInZone = false
end)
```

---

## Session 2：UI 提示 (40分钟)

### 2.1 创建 ScreenGui (10分钟)

```
1. 在 StarterGui 下创建 ScreenGui
2. 重命名: BusDriverUI
3. 在 BusDriverUI 下创建 Frame
4. Frame 设置:
   Name: StopInfoFrame
   Size: {0.3, 0}, {0.15, 0}
   Position: {0.35, 0}, {0.02, 0}
   BackgroundColor3: RGB(0, 0, 100)
   BackgroundTransparency: 0.3
```

### 2.2 添加文字 (10分钟)

```
在 StopInfoFrame 下创建 TextLabel:
   Name: NextStopLabel
   Size: {1, 0}, {0.5, 0}
   Position: {0, 0}, {0, 0}
   Text: "下一站: Richmond"
   TextColor3: 白色
   TextScaled: ✓
   BackgroundTransparency: 1

再创建一个 TextLabel:
   Name: RouteLabel
   Size: {1, 0}, {0.5, 0}
   Position: {0, 0}, {0.5, 0}
   Text: "65 → Ealing Broadway"
   TextColor3: 黄色
   TextScaled: ✓
   BackgroundTransparency: 1
```

### 2.3 用脚本更新 UI (20分钟)

**创建 RemoteEvent：**
```
1. 在 ReplicatedStorage 下创建 RemoteEvent
2. 重命名: UpdateStopUI
```

**修改站点检测脚本，加上：**
```lua
-- 在检测到巴士后加这行：
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local updateEvent = ReplicatedStorage:FindFirstChild("UpdateStopUI")
if updateEvent then
    updateEvent:FireAllClients(stopName)
end
```

**在 StarterGui/BusDriverUI 下创建 LocalScript：**
```lua
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local updateEvent = ReplicatedStorage:WaitForChild("UpdateStopUI")

local frame = script.Parent:WaitForChild("StopInfoFrame")
local nextStopLabel = frame:WaitForChild("NextStopLabel")

updateEvent.OnClientEvent:Connect(function(stopName)
    nextStopLabel.Text = "已到达: " .. stopName
    wait(3)
    nextStopLabel.Text = "下一站: ???"
end)
```

---

## Session 3：测试 (20分钟)

### 3.1 完整测试流程

```
1. F5 进入游戏
2. 上巴士
3. 开到 Richmond 站
4. 进入检测区
5. 看 Output 有没有显示
6. 看 UI 有没有变化
7. 开出去再开回来，测试重复检测
```

### ✅ Day 6 完成检查

- [ ] 到站检测脚本工作
- [ ] UI 显示当前站点
- [ ] Output 显示到站信息
- [ ] 保存项目

---

# Day 7：车门系统

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 创建门 | 30分钟 |
| Session 2 | 开关门脚本 | 40分钟 |
| Session 3 | 键盘控制 | 30分钟 |

---

## Session 1：创建门 (30分钟)

### 1.1 找到或创建门 (15分钟)

**如果巴士模型有门：**
- 在 Explorer 里找到门的 Part
- 记住它的名字

**如果没有门，创建一个：**
```
Part:
Name: FrontDoor
Size: 2, 4, 0.3
Position: 放在巴士前门位置
Material: SmoothPlastic
Color: 红色（和巴士一样）
Anchored: false
```

### 1.2 焊接门到车身 (15分钟)

```
1. 选中门
2. 右键 → Insert Object → WeldConstraint
3. Part0 = 门
4. Part1 = 车身
```

**现在门跟着巴士一起动了！**

---

## Session 2：开关门脚本 (40分钟)

### 2.1 创建 RemoteEvents (5分钟)

```
在 ReplicatedStorage 下创建:
- RemoteEvent: OpenDoorsEvent
- RemoteEvent: CloseDoorsEvent
```

### 2.2 车门控制脚本 (35分钟)

**在巴士 Model 下创建 Script：**

```lua
--[[
    车门控制脚本
    Day 7 制作
    
    O = 开门, C = 关门
]]

local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local bus = script.Parent
local frontDoor = bus:FindFirstChild("FrontDoor")

if not frontDoor then
    warn("⚠️ 找不到 FrontDoor！")
    return
end

print("✅ 车门控制脚本加载成功")

-- 设置
local OPEN_DISTANCE = 2  -- 门打开移动的距离
local OPEN_TIME = 0.8    -- 开门时间（秒）

-- 状态
local doorOpen = false

-- 保存门的初始位置
local doorClosedCFrame = frontDoor.CFrame

-- 动画设置
local tweenInfo = TweenInfo.new(OPEN_TIME, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

-- 开门
local function openDoor()
    if doorOpen then return end
    doorOpen = true
    print("🚪 开门")
    
    local targetCFrame = doorClosedCFrame * CFrame.new(OPEN_DISTANCE, 0, 0)
    local tween = TweenService:Create(frontDoor, tweenInfo, {CFrame = targetCFrame})
    tween:Play()
end

-- 关门
local function closeDoor()
    if not doorOpen then return end
    doorOpen = false
    print("🚪 关门")
    
    local tween = TweenService:Create(frontDoor, tweenInfo, {CFrame = doorClosedCFrame})
    tween:Play()
end

-- 监听事件
local openEvent = ReplicatedStorage:WaitForChild("OpenDoorsEvent")
local closeEvent = ReplicatedStorage:WaitForChild("CloseDoorsEvent")

openEvent.OnServerEvent:Connect(openDoor)
closeEvent.OnServerEvent:Connect(closeDoor)

print("🚪 按 O 开门, C 关门")
```

---

## Session 3：键盘控制 (30分钟)

### 3.1 创建 LocalScript (30分钟)

**在 StarterPlayerScripts 下创建 LocalScript：**

```lua
--[[
    键盘控制脚本
    处理玩家按键
]]

local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local openEvent = ReplicatedStorage:WaitForChild("OpenDoorsEvent")
local closeEvent = ReplicatedStorage:WaitForChild("CloseDoorsEvent")

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.O then
        print("按了 O")
        openEvent:FireServer()
    elseif input.KeyCode == Enum.KeyCode.C then
        print("按了 C")
        closeEvent:FireServer()
    end
end)

print("✅ 键盘控制脚本加载成功")
print("🚪 O = 开门, C = 关门")
```

### ✅ Day 7 完成检查

- [ ] 有可以动的门
- [ ] 按 O 能开门
- [ ] 按 C 能关门
- [ ] 门有动画效果
- [ ] 保存项目

---

# Day 8：第二个站点 + 完善

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 复制创建 Kingston 站 | 40分钟 |
| Session 2 | 连接道路 | 30分钟 |
| Session 3 | 装饰和完善 | 50分钟 |

---

## Session 1：Kingston 站 (40分钟)

### 1.1 复制 Richmond 站 (10分钟)

```
1. 选中 BusStop_Richmond
2. Ctrl+D 复制
3. 重命名为 BusStop_Kingston
4. 移动到道路起点
```

### 1.2 修改属性 (10分钟)

```
1. 点击 BusStop_Kingston
2. 修改 Attributes:
   - StopId: kingston
   - StopName: Kingston
3. 修改站牌上的文字
```

### 1.3 微调外观 (20分钟)

**Kingston 特色：**
- 购物中心风格
- 可以加个大招牌 "Kingston"

---

## Session 2：连接道路 (30分钟)

### 2.1 延长道路

```
1. 从 Richmond 延长道路到 Kingston
2. 或者 Kingston 作为起点
3. 加一点弯道让线路有变化
```

### 2.2 调整站点位置

确保两个站都：
- 靠近道路
- 巴士能停进站
- 检测区覆盖停车位置

---

## Session 3：装饰完善 (50分钟)

### 3.1 从 Toolbox 添加装饰 (30分钟)

搜索并添加：
- [ ] 路灯 (street light)
- [ ] 树 (tree)
- [ ] 长椅 (bench)
- [ ] 垃圾桶 (trash bin)
- [ ] 红绿灯 (traffic light)

### 3.2 添加 NPC 乘客 (20分钟)

```
1. Toolbox 搜 "NPC" 或 "R15 rig"
2. 放几个在公交站候车
3. 不需要动，站着就行
```

### ✅ Day 8 完成检查

- [ ] Kingston 站完成
- [ ] 道路连接两个站
- [ ] 有路灯和树
- [ ] 有候车乘客
- [ ] 完整跑一遍路线
- [ ] 保存项目

---

# Day 9：发布和分享！🎉

## ⏰ 时间安排
| 时段 | 内容 | 时长 |
|------|------|------|
| Session 1 | 最终测试和修复 | 40分钟 |
| Session 2 | 发布准备 | 30分钟 |
| Session 3 | 发布和分享 | 30分钟 |
| Session 4 | 庆祝！ | ∞ |

---

## Session 1：最终测试 (40分钟)

### 1.1 完整游戏流程测试

```
□ 游戏启动正常
□ 玩家能找到巴士
□ 能上车
□ WASD 控制正常
□ 从 Kingston 出发
□ 到站检测工作
□ 车门能开关
□ 到达 Richmond
□ UI 显示正确
□ 没有明显 bug
```

### 1.2 请家人测试

让爸爸/妈妈玩一遍，看他们能不能：
- 找到并上车
- 开到目的地
- 记录任何困惑的地方

### 1.3 修复问题

列出问题并修复...

---

## Session 2：发布准备 (30分钟)

### 2.1 游戏图标 (10分钟)

```
1. 在游戏里调整到好看的角度
2. View → Screenshot
3. 或者用画图软件做一个
4. 尺寸: 512x512
```

### 2.2 游戏描述 (10分钟)

```
🚌 London Bus 65 Simulator

开着伦敦双层巴士，从 Kingston 到 Richmond！

🎮 操作:
- WASD: 驾驶
- O: 开门
- C: 关门

✨ 特色:
- 真实伦敦巴士
- 真实站点名称
- 到站提示系统

Made by [你的名字], Age 12
Half Term 2026 作品
```

### 2.3 截图 (10分钟)

截 3-5 张好看的图：
1. 巴士全景
2. 驾驶视角
3. 车站
4. 到站UI
5. 整体地图

---

## Session 3：发布！(30分钟)

### 3.1 发布到 Roblox (10分钟)

```
1. File → Publish to Roblox
2. 如果是第一次: Create New Game
3. 填写:
   - Name: London Bus 65 Simulator
   - Description: (上面写的)
   - Genre: Town and City
4. 点 Create
```

### 3.2 设置为 Friends Only (5分钟)

```
1. 发布后，去 Roblox 网站
2. 找到你的游戏
3. Configure → Access
4. 选 Friends Only
5. Save
```

### 3.3 分享给朋友 (15分钟)

```
1. 复制游戏链接
2. 发给朋友们
3. 把他们加为 Roblox 好友
4. 让他们玩！
```

---

## Session 4：庆祝！🎉

**你完成了！**

你在 9 天里：
- ✅ 学会了 Roblox Studio
- ✅ 写了 Lua 代码
- ✅ 做了一辆能开的巴士
- ✅ 建了两个真实站点
- ✅ 创建了游戏系统
- ✅ 发布了自己的游戏
- ✅ 分享给朋友玩

**这是非常了不起的成就！** 🏆

### 收集反馈

问朋友们：
- 最喜欢什么？
- 哪里有问题？
- 想要什么新功能？

### 未来可以加的功能

- [ ] 更多站点
- [ ] 乘客上下车
- [ ] 计分系统
- [ ] 时刻表
- [ ] 其他路线
- [ ] 多人联机

---

# 🎊 恭喜完成！

你现在是一个 **Roblox 游戏开发者** 了！

继续创造吧！🚀
